# ====================================
# Apache Zookeeper - managed by Puppet
# ====================================

dataDir=<%= scope.lookupvar('::zookeeper::config_datadir')%>/

<% scope.lookupvar('::zookeeper::config_options').each do |key,value| -%>
<%= key %>=<%= value %>
<% end -%>
